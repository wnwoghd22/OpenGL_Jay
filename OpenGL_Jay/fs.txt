#version 330 core

in vec3 colorVec;
in vec2 texCoord;

out vec4 FragColor;

//uniform sampler2D sTexture;
uniform sampler2D texture1;
uniform sampler2D texture2;

uniform bool tex1;
uniform bool tex2;

void main()
{
    //FragColor = texture(sTexture, texCoord);
    //FragColor = texture(sTexture, texCoord) * vec4(colorVec, 1.0);
    //exercise1
    vec2 tex2coord = vec2(texCoord.x, -texCoord.y);
    FragColor = mix(texture(texture1, texCoord) * vec4(tex1 ? 1.0 : 0.0), texture(texture2, tex2coord)* vec4(tex2 ? 1.0 : 0.0), 0.2);

    //FragColor = texture(texture1, vec2(texCoord.x, -texCoord.y)) * vec4(colorVec, 1.0);
}